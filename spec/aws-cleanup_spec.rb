require "spec_helper"

describe AwsCleanup do
  it "has a version number" do
    expect(AwsCleanup::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
